# Вопросы по UNICO v3.0 и работе над ним

## Архитектура и профили

### Базовые вопросы
1. Какие профили (E0–E7) существуют в UNICO v3.0 и что каждый из них делает?
2. Каков формат байтов модуля UNICO? Что означает магическое число `UNICO\x07`?
3. Что такое ULEB-кодирование и как оно используется в UNICO?
4. Каков правильный паттерн skip-continuation для ULEB и почему он важен?
5. Чем отличается `uleb_len` от простого подсчёта байт?
6. Какие типы ошибок существуют в UNICO (`crate::Error`)?
7. Что такое `ExecutionResult` и как он отличается от `Trap()`?
8. Как работает механизм `Provenance` в UNICO?

### E0 — Базовый профиль
9. Какие инструкции есть в E0 и какие opcodes у них?
10. Как E0 обрабатывает вызов функций (CALL) и возврат (RET)?

### E1 — Ветвление
11. Какие инструкции ветвления реализованы в E1?
12. Как работает `BR.IF` с условным переходом?

### E2 — Память
13. Как E2 управляет памятью? Сколько страниц и какого размера?
14. Что происходит при обращении по несуществующему адресу (OOB)?

### E3 — Арифметика и память
15. Какие арифметические операции есть в E3? (SUB, MUL, DIV, и др.)
16. Как E3 реализует загрузку/сохранение 32/64-битных значений?
17. Что такое `MemSize` и что он возвращает — байты или страницы?
18. Как `build_e7` должен записывать `func_count` в модуль?

### E5 — SIMD (128-бит векторы)
19. Как устроена SIMD-архитектура E5? Сколько lanes и какой формат?
20. Как E5 отслеживает `last_vreg` и что возвращает RET?
21. Почему VStore и VMemZero не обновляют `last_vreg`?

### E6 — Фиксированная точка (Q8.8)
22. Что такое Q8.8 формат и как он хранится?
23. Как работает QMUL: `(a*b) >> 8`?
24. Как работает QDIV: `(a<<8) / b`?
25. Почему QADD/QSUB используют saturating arithmetic?
26. Что возвращает RET в E6 — сырое Q8.8 значение или плавающее?

### E7 — Криптография
27. Сколько страниц памяти и crypto_slots в E7?
28. Какие криптографические примитивы реализованы в E7?
29. Каковы opcodes для Hash, HMAC, KDF, Mul, AddMod, ModExp?
30. Чем crypto_slots отличаются от обычных регистров?
31. Почему `crypto_slots[*x].0[..32]` даёт `&[u8]`, а не `&[u8; 32]`?
32. Как E7 реализует AES-128/256 (S-box, key expansion, Round Key)?
33. Как BLAKE2s-256 работает внутри E7 (10 раундов, sigma permutation)?
34. Как HMAC-SHA256 реализован в E7 (inner/outer pad)?
35. Как HKDF-SHA256 реализован (Extract + Expand)?
36. Как E7 реализует 256-битное умножение (Mul)?
37. Как `mul_mod_limbs` работает — schoolbook умножение + binary long division?
38. Как реализован `modexp_limbs` — binary exponentiation?
39. Что такое `byte_div_mod` и как она работает (512 итераций)?
40. Как `add_mod_limbs` обрабатывает перенос (carry propagation)?

## Код и реализация

### Rust runtime
41. Как организована модульная структура rust-runtime?
42. Как `mod.rs` экспортирует профили E0–E7?
43. Какую роль играет `Frame` структуре в E7?
44. Как `thiserror` используется для обработки ошибок?
45. Каков паттерн `build_e7` → `parse` для создания/чтения модулей?

### Тестирование
46. Сколько тестов проходит для каждого профиля?
47. Как проверяется SHA-256 с известным хешем ("abc" → ba7816bf...)?
48. Как тестируется AES roundtrip с известным ключом и plaintext?
49. Как проверяется AddMod (2+3 mod 5 = 0)?
50. Как проверяется Mul (2×3 = 6) и ModExp (2^10 mod 7 = 6)?

## Мои работы и вклад

### Разработка E7
51. Какие новые инструкции были добавлены в E7 (Hash, HMAC, KDF, Mul, AddMod, ModExp)?
52. Какие helper-функции были реализованы (blake2s256, hmac_sha256, hkdf_sha256, sha256_of_slice)?
53. Как были решены проблемы с типами `crypto_slots` (`[u8; 64]` vs `&[u8; 32]`)?
54. Как был реализован big-integer arithmetic (4×u64 limbs)?
55. Какие compile-time ошибки встретились и как были исправлены?
56. Как было исправлено `build_e7` — недостающий `func_count` на позиции 6?
57. Как был исправлен `func_payload` (15 вместо 16)?
58. Как был исправлен ULEB continuation-skip паттерн?
59. Как `Rand` записывает результат в `regs[dst]` для корректного Ret?
60. Как `u64::from_le_bytes` требует ровно 8 байт — как это было решено?

### Паттерны и решения
61. Какой финальный паттерн ULEB continuation-skip был установлен?
62. Почему `key_bits` в E7 должен быть `u16`, а не `u8`?
63. Как был решён конфликт mutable/immutable borrow в AESEnc/AESDec/XOR?
64. Как реализован `mul_add` с `wrapping_add` для корректного сложения?
65. Как `limbs_to_bytes` и `bytes_to_limbs` конвертируют между 32 байтами и 4×u64?

## Перспективы и следующие шаги

66. Какие криптографические операции ещё не реализованы в E7? (RSA, ECC?)
67. Какие оптимизации можно внести в `byte_div_mod` для big-int arithmetic?
68. Как можно интегрировать внешние криптобиблиотеки (OpenSSL, ring) вместо software implementation?
69. Каков план по созданию assembler/compiler для UNICO модуля?
70. Как можно расширить E7 для поддержки ECC (Elliptic Curve Cryptography)?
71. Как добавить постквантовые алгоритмы (Kyber, Dilithium) в E7?

## Теория и концепции

73. Что такое ULEB128 и SLEB128 кодирование?
74. Как работает AES-128 vs AES-256 (количество раундов, размер key schedule)?
75. В чём разница между SHA-256 и BLAKE2s?
76. Как работает HMAC и почему используются два прохода?
77. Как работает HKDF (HMAC-based Key Derivation Function)?
78. Что такое schoolbook multiplication и binary long division для big-int?
79. Как работает binary exponentiation (square-and-multiply)?
80. Что такое GF(2^8) и как AES использует конечное поле?
81. Как устроен sigma permutation в BLAKE2s?
82. Что такое «carry propagation» в модульной арифметике?

## Интеграция и экосистема

83. Как UNICO взаимодействует с ssp4-rs (Rust SSP5)?
84. Какую роль играет bha-codecs-core в контексте UNICO?
85. Как Portable OS (D:\4) связан с UNICO?
86. Как UNICO v3.0 отличается от v2.7?
87. Каковы требования к rust-runtime (Cargo.toml зависимости)?
88. Как запустить тесты и benchmarks для UNICO?
